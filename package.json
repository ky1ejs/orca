{
  "name": "orca",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --names backend,web --prefix-colors blue,green \"cd backend && bun run dev\" \"cd web && bun run dev\"",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "validate": "bun run lint && bun run format:check && cd shared && bun run validate && cd ../backend && bun run validate && cd ../web && bun run validate"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.28.0",
    "@typescript-eslint/parser": "^8.28.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.22.0",
    "eslint-plugin-react": "^7.37.4",
    "eslint-plugin-react-hooks": "^5.2.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  }
}
